export class Lexer {
    private hasError = false

    constructor(private source: string) { }

    scanTokens() {
        // if error report it back then gather some useful info then halt
        // but "gather some useful" is too hard 
    }
}
